apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: api-denartcc-org-tls
spec:
  dnsNames:
  - api.zdvartcc.org
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt
  secretName: api-denartcc-org-tls
